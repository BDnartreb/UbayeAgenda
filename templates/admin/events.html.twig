{% extends 'front.html.twig' %}

{% block title %}UbayeAgenda{% endblock %}

{% block content %}
    <div class="main-container">
        <div class="left-container">
            {% if app.user %}
                <div><h1>Bienvenue {{ app.user.name }} !</h1></div>
            {% endif %}
            <div><h2>Filtrer par :</h2></div>
            <form id="filters-form" method="get" action="{{ path('admin_events') }}">      
                <div class="filter-group">
                    <input type="checkbox" id="toggle-organisateur" hidden>
                    <label for="toggle-organisateur" class="filter-title">Organisateur</label>
                    <div class="filter-options {% if selectedOrganisations is not empty %}open{% endif %}">
                        {% for organisation in organisations %}
                            <label>
                                <input 
                                    type="checkbox" 
                                    name="organisations[]" 
                                    value="{{ organisation.name }}"
                                    {% if organisation.name in selectedOrganisations %}checked{% endif %}
                                >
                                {{ organisation.name }}
                            </label><br>
                        {% endfor %}
                    </div>
                </div>

                {% set today = 'now'|date('Y-m-d') %}
                <div class="filter-group">
                    <input 
                        type="checkbox" 
                        id="toggle-date" 
                        hidden
                        {% if selectedDate != today %}checked{% endif %}
                    >

                    <label for="toggle-date" class="filter-title">Date</label>

                    <div class="filter-options">
                        <input 
                            type="date"
                            id="selectedDate"
                            name="selectedDate"
                            value="{{ selectedDate }}"
                        >
                    </div>
                </div>
                <button type="submit" class="btn btn-danger">Filtrer</button>
            </form>
        </div>

        <div class="central-container">
        {% for event in events %}
            <div class="item-container"> 
                <a href="{{ path('event', {'id': event.id}) }}">
                    <div class="event-small-card">
                        <div class="event-poster">
                            <img src="{{ asset(event.poster) }}" alt="Affiche de l'événement">
                        </div>
                        <div class="event-small-card-data">
                            <div class=""><h2> {{ event.name }} </h2></div>
                            <div class=""><h4>{{ event.startDate|date('l d/m/Y - H:i')  }} à {{ event.endDate|date('H:i')  }}</h4></div>
                            <div class=""><h4>{{ event.location.name }} - {{ event.location.town.value }}</h4></div>
                            <div class=""><h4>{{ event.organisation.name}}</h4></div>
                        </div>
                    </div>
                </a>
                <div class="btn-container">
                    <div>
                        <a href="{{ path('organisation_event_update', {'id': event.id}) }}" class="btn-link btn-link-modify">Modifier</a>
                    </div>
                    <div>
                        <form method="post" action="{{ path('organisation_event_delete', {'id': event.id}) }}" style="display:inline;" onsubmit="return confirm('Voulez-vous vraiment supprimer cet événement ?');">
                            <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ event.id) }}">
                            <button type="submit" class="btn-link btn-link-delete">Supprimer</button>
                        </form>
                    </div>
                </div>
            </div>
        {% endfor %}
    </div> 

{#     
        <div class="agenda-container">
            <div class="day-container">
                {% for event in events %}
                <div class="day">
                    <div class="event_name"><h1> {{ event.name }} </h1></div>
                    <a href="{{ path('event', {'id': event.id}) }}">Voir</a>
                    <a href="{{ path('admin_event_update', {'id': event.id}) }}">Modifier</a>
                    <form method="post" action="{{ path('admin_event_delete', {'id': event.id}) }}" style="display:inline;" onsubmit="return confirm('Voulez-vous vraiment supprimer cette organisation ?');">
                        <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ event.id) }}">
                        <button type="submit" class="btn-link">Supprimer</button>
                    </form>
                </div>
                {% endfor %}
                <div class="events">
                    <div class="event-card">
                        <div class="event-poster">
                            <img src="{{ asset('images/poster.jpg') }}" alt="Affiche de l'événement">
                        </div>
                        <div class="event-info">
                            <div class="event-time-location"><h2>11h30 - Lieu de l'Event</h2></div>
                            <div class="event-name"><h2>Event Name</h2></div>
                            <div class="event-organizer"><h3>Présenté par : Organisateur Name</h3></div>
                            <div class="event-moreinfo"><h4>Spectacle - Gratuit - Tout public</h4></div>
                        </div>
                    </div>
                </div>
            </div> 
        </div> #}
    </div>

{% endblock %}
