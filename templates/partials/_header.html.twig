    <header>
        <nav class="navbar navbar-expand-lg custom-navbar">
            <div class="header-container">
                <div class="logo">Ubaye Agenda</div>
                <button class="navbar-toggler" data-bs-toggle="collapse" data-bs-target="#menu">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="collapse navbar-collapse" id="menu">
                    <ul class="navbar-nav">
                         <li class="navbar-item">
                            <a href="{{ path('home') }}" class="nav-link">Agenda</a>
                        </li> 
                        {# <li class="navbar-item">
                            <a href="#" class="nav-link">Partenaires</a>
                        </li> #}
                        <li class="navbar-item">
                            <a href="{{ path('about') }}" class="nav-link">A propos</a>
                        </li> 
                        <li class="navbar-item">
                            <a href="{{ path('contact') }}" class="nav-link">Contact</a>
                        </li> 
                        <li class="navbar-item">
                            <a href="{{ path('login') }}" class="nav-link">Se Connecter</a>
                        </li> 
                    </ul>
                </div>
            </div>
        </nav>
    </header>