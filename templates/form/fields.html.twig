{% block form_label %}
    {% if label is not same as(false) %}
        {% set label_text = label|default('') %}
        {% if required %}
            {% set label_text = label_text ~ ' *' %}
        {% endif %}
        <label {{ block('attributes') }}>{{ label_text|raw }}</label>
    {% endif %}
{% endblock %}
